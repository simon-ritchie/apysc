# `apysc._mask.svg_mask_mixin` docstrings

## Module summary

The SVG mask property-related mix-in class implementation.

## `SvgMaskMixIn` class docstring

The SVG mask property-related mix-in class.